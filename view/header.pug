nav.nav-links.row
  ul.navbar
    li.features
      a.nav(href='/') Home
    li.Plans
      a.nav(href='/plans') PLANS
    if name
       li.name
         a.pro-name(href='/profile' ) #{name} Profile
       li.logout
         a.nav(href='/logout' ) LOGOUT
    else     
       li.signup-nav
         a.nav(href='/login') LOGIN
   
    
  
